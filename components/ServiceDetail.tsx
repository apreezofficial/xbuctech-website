import type { Service } from "@/lib/services";
import { ServiceLink } from "./ServiceLink";

export function ServiceDetail({ service }: { service: Service }) {
  return <main className="service-detail shell" style={{ "--service-accent": service.accent } as React.CSSProperties}><div className="detail-breadcrumb"><a href="/services">Services</a><span>/</span><span>{service.shortTitle}</span></div><div className="detail-layout"><div><p className="eyebrow">{service.eyebrow}</p><h1>{service.title}</h1><p className="detail-lede">{service.description}</p><ServiceLink href="/contact" className="button button-light">Talk to an expert</ServiceLink></div><div className="detail-marker"><span>XB</span><strong>BUILT<br />AROUND<br /><em>YOU.</em></strong></div></div><section className="detail-content"><div><p className="eyebrow">/ The work</p><h2>Clarity before<br /><em>complexity.</em></h2></div><div><p className="detail-copy">{service.detail}</p><div className="capability-list detail-list">{service.capabilities.map((capability, index) => <div className="capability" key={capability}><span>0{index + 1}</span><strong>{capability}</strong><span>↗</span></div>)}</div></div></section><section className="detail-cta"><p className="eyebrow">/ Ready when you are</p><h2>Make your next<br /><em>move secure.</em></h2><ServiceLink href="/contact" className="button button-dark">Schedule a consultation</ServiceLink></section></main>;
}
